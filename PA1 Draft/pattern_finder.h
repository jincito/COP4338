#include <stdbool.h>
#include <stdio.h>
#include <string.h>
#define LINE_LEN 1000
bool is_palindrome(char line[]); // function declaration!
bool is_singleton(char line[]);
bool is_arithmetic(char line[]);
bool is_reverse_arithmetic(char line[]);
bool is_bipartite(char line[]);
bool is_tripartite(char line[]);
